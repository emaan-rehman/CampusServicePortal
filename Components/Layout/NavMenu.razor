@using CampusServicePortal.Data
@inject UserSession Session

<nav class="flex-column">
    <div class="nav-item px-3">
        <NavLink class="nav-link" href="" Match="NavLinkMatch.All">Dashboard</NavLink>
    </div>

    @if (Session.IsLoggedIn)
    {
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Complaints">Complaints</NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="transport">Transport</NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="events">Events</NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Library">Library</NavLink>
        </div>

        @* Temporarily show this to everyone logged in to see if it works *@
        <div class="nav-item px-3 border-top mt-2">
            <NavLink class="nav-link text-warning" href="RoleManagement">Admin Panel</NavLink>
        </div>
    }
</nav>